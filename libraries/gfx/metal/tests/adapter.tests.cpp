//#include <morpheus/gfx/metal/adapter.hpp>
#include <catch2/catch_test_macros.hpp>

namespace morpheus::gfx::metal
{

TEST_CASE("Create an adapter mode list", "[morpheus.gfx.metal.adapter]")
{
}

TEST_CASE("Iterates over the adapters in the list", "[morpheus.gfx.metal.adapter]")
{
    GIVEN("An adapter list")
    {
//        Adapter adapters;
        THEN("Loop over all adapters using native for loop syntax")
        {
            //for (const auto& adapter : adapters)
            {

            }
        }
    }
}

} // namespace morpheus::test
